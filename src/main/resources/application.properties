spring.application.name=paralelismo
server.port=8080

# Endpoints podem demorar (experimentos completos), aumentamos o timeout
# do servidor embutido para evitar corte de conexao em testes manuais.
server.tomcat.connection-timeout=600000
